import React from 'react';

const Header = () => {
  return (
    <header className="app-header">
      <h1>Anime Explorer</h1> {/* Display the title of the app */}
    </header>
  );
};

export default Header;
